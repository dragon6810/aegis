#ifndef _GLOBALS_H
#define _GLOBALS_H

#include <stddef.h>
#include <limits.h>
#include <bspfile.h>

extern char sourcefilepath[PATH_MAX];
extern char destfilepath[PATH_MAX];
extern char entfilepath[PATH_MAX];
extern char outfilepaths[MAX_MAP_HULLS][PATH_MAX];

#endif